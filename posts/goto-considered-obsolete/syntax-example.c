#include <stdio.h>
#include <stdlib.h>
int main(int argc, char **argv) {
  int max = atoi(argv[1]);
  printf("%d\n", run int loop(int i = 0, int sum = 0) {
    if (i < max)
      return loop(i + 1, sum + i);
    else
      return sum;
  });
  return 0;
}
